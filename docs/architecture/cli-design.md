# CLI Design

- Command structure: `confl <action> [options]`
- Non-interactive by default—designed for scripting and agents
- Human-readable output by default (Rich), `--json` flag for machine parsing
