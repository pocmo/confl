---
id: c-05e2
status: closed
deps: []
links: []
created: 2026-01-14T20:44:41Z
type: task
priority: 0
assignee: Sebastian Kaspari
---
# Implement page get command with Rich rendering

Implement the 'confl page get' command to fetch and display pages in the terminal using Rich Markdown rendering.

From content-rendering.md research:
- Use storage format from API (already fixed in c-da30)
- Convert storage → Markdown using storage_to_markdown()
- Render Markdown with Rich for beautiful terminal output
- Default output should be Rich-rendered
- Handle errors gracefully

Implementation:
1. Create page get command handler
2. Fetch page with body.storage
3. Convert storage_to_markdown()
4. Render with Rich's Markdown renderer
5. Add error handling for API failures

Acceptance:
- 'confl page get <page-id>' displays formatted page
- Rich colors, headings, lists, code blocks render nicely
- Macros convert to Markdown equivalents (panels, code, status)
- Unknown macros don't crash, show text or skip gracefully


## Notes

**2026-01-14T20:48:51Z**

Completed: Implemented Rich Markdown rendering for page get command. Storage format is converted to Markdown using storage_to_markdown(), then rendered with Rich's Markdown renderer for beautiful terminal output. Updated --markdown flag to output converted markdown. Added tests for Rich rendering and markdown output. All tests pass.
