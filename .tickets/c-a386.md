---
id: c-a386
status: closed
deps: []
links: []
created: 2026-01-15T13:28:30Z
type: bug
priority: 0
assignee: Sebastian Kaspari
---
# FIX: Failing tests must be resolved - no shortcuts

Tests are currently failing. Fix the root causes properly and ensure all tests pass.

## ⚠️ CRITICAL STANDARDS ⚠️

**Tests are NOT optional. They MUST pass.**

- Fix the ROOT CAUSE of failures, not symptoms
- No shortcuts, no commenting out tests
- No skipping tests without extremely good reason
- Be professional and thorough
- Tests are documentation and safety net

## Tasks
1. Run `uv run pytest` to see all failures
2. For EACH failing test:
   - Read the failure message carefully
   - Understand what the test expects
   - Identify the root cause (code bug vs test bug)
   - Fix the actual problem properly
   - Verify the fix doesn't break other tests
3. Run `uv run pytest -v` to see detailed output
4. If needed, run specific tests: `uv run pytest tests/test_file.py::test_name`
5. Ensure ALL tests pass before closing ticket
6. Check test coverage hasn't dropped: `uv run pytest --cov=src/confl`

## Common Root Causes to Check
- API endpoints changed but mocks not updated
- Function signatures changed but tests not updated
- New code added without updating affected tests
- Test fixtures out of sync with code
- Import errors or missing dependencies
- Type errors that tests catch

## Acceptance Criteria
- `uv run pytest` exits with code 0
- ALL tests pass (no skips unless justified)
- No test warnings (address them or justify)
- Test coverage remains high (>90%)
- Root causes fixed, not symptoms masked
- Code quality maintained

## Professional Standards
- Read error messages completely
- Understand before fixing
- Test your fixes
- Don't rush
- Don't take shortcuts
- Tests protect everyone

## References
- tests/ — all test files
- pytest.ini or pyproject.toml — test configuration
- .ralph/prompt.md — verification requirements

## Notes
**TESTS MUST PASS BEFORE ANY TICKET IS CLOSED.**
This is non-negotiable. Failing tests mean broken functionality.
Fix them properly and professionally.


**2026-01-15T13:32:00Z**

FIXED: All 8 test failures resolved by addressing root causes:
1. Auth/config tests (5 failures) - Fixed clean_env fixture to properly isolate tests from real credentials file by mocking get_credentials_path()
2. Space tests (3 failures) - Fixed user endpoint URL mocks from v2 (/wiki/api/v2/user/current) to v1 (/wiki/rest/api/user/current) to match actual implementation
All 699 tests now pass. No shortcuts taken - proper root cause fixes.
